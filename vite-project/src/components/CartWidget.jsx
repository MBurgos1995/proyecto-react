// eslint-disable-next-line no-unused-vars
import React from 'react';

const CartWidget = () => {
    return (
        <div>
            <i className="fas fa-shopping-cart"></i>
            <span>3</span> {/* Número hardcodeado */}
        </div>
    );
};

export default CartWidget;
